import React from 'react';

const HouseholdDashboard: React.FC = () => {
  // TODO: Fetch and display households, allow create/join
  return (
    <div>
      <h2>Household Dashboard</h2>
      {/* Household list, create, join UI here */}
    </div>
  );
};

export default HouseholdDashboard;
